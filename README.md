I got so done with readthedocs themes that I've decided to make my own. This theme should look like a visual studio code window but in readthedocs

## Installation

```bash
pip install rtdvscodetheme
